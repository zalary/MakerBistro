<%= form_for([@menu, @ingredient], url: menu_ingredient_path) do |f| %>
  <% require 'pry'; binding.pry %>
  <%= f.label :name %>
  <%= f.text_field :name %><br />
  <%= submit_tag("submit") %>
<% end %>
