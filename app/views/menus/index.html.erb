<% @menus.each do |menu| %>
  Name: <%= menu.name %>,
  Price: <%= menu.price %>,
  Ingredients: <% menu.ingredients.each do |i| %>
    <%= i.name %>
  <% end %><br/>
  <%= link_to 'Ingredients', new_menu_ingredient_url(menu.id) %>
<% end %>
